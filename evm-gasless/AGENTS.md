# Project Agent Instructions

## 保存文档

当用户说“保存文档”时：

1. 将当前对话中最近一段需要保存的分析、说明或查询结果整理成 Markdown。
2. 保存到本项目的 `docs/` 目录。
3. 使用能够表达主题的英文 kebab-case 文件名，并以 `.md` 结尾。
4. 保留有用的标题、表格、代码块、Mermaid 图和相关链接。
5. 如果目标文件已经存在，更新对应文档；不要无提示地覆盖无关文档。
6. 除非无法判断需要保存的内容，否则不再询问目录或文件格式。

